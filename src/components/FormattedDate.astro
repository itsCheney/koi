---
interface Props {
    date: Date;
    class?: string;
}

const {date, class: className = ""} = Astro.props;
---

<time datetime={date.toISOString()} class={className}>{
    // 使用 .toISOString().slice(0, 10) 来获取 YYYY-MM-DD 格式
    date.toISOString().slice(0, 10)
}</time>